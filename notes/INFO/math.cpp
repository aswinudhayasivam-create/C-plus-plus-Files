#include <iostream>
#include <cmath>
using namespace std;

int main() {
  cout << "\nSquare root of 64 is: " << sqrt(64) << "\n";
  cout << "\nRound 2.6 to the nearest integer: " << round(2.6) << "\n";
  cout << "\nThe natural logarithm of 2 is: " <<log(2) << "\n";
 cout << min(5, 10);
 cout << "\n";
  cout << max(5, 10);
    cout << "\nSquare root of 4 is: " << sqrt(4) << "\n";
  cout << "\nRound 2.3 to the nearest integer: " << round(2.3) << "\n";
  cout << "\nThe natural logarithm of 1 is: " <<log(1) << "\n";
  return 0;
}
